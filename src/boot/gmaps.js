import { x5GMaps, gmaps } from 'x5-gmaps'

// "async" is optional;
// more info on params: https://quasar.dev/quasar-cli/boot-files
export default async ({ app, router, Vue }) => {
  // Vue.use(x5GMaps, { key: 'XXXXXX', libraries: ['places'] })
}
